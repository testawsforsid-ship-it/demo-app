pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/siddubellanki/demo-app.git',
            }
        }

        stage('Build') {
            steps {
                echo 'Build successful'
            }
        }
    }
}
